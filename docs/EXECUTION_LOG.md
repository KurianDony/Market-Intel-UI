# Execution Log

| Timestamp | Step | Notes |
|---|---|---|
| 2026-05-09 22:58 | P0.1 done | Repo at github.com/KurianDony/Market-Intel-UI, npm run dev confirmed running |
| 2026-05-09 22:58 | P0.2 done | shadcn already initialized by starter template — style: new-york, base: neutral, CSS vars on; components/ui has badge, button, card, checkbox, dropdown-menu, input, label |
| 2026-05-09 23:00 | Director call | P0.3 deferred — ~250 NSW SA2 polygons load fine as static GeoJSON in public/; no PMTiles/R2 needed at this scale. Will revisit in Phase 1.x when choropleth overlays required. BUILD_PLAN.md updated. |
| 2026-05-09 23:12 | P0.4 done | Mapbox style chosen: mapbox://styles/kuriandony/cmoyd2k8u000q01su80hf8dwa (Dark 2D fork, unmodified — label tuning deferred to P1.3). Public token wired to NEXT_PUBLIC_MAPBOX_TOKEN + NEXT_PUBLIC_MAPBOX_STYLE in .env.local. |
| 2026-05-09 23:48 | Director call | Lovable repo connection dropped from Phase 0. Sandbox-only; pages prototyped ad-hoc in Phase 2+, code copied to main repo by hand. BUILD_PLAN.md P0.5 revised. |
| 2026-05-09 23:48 | P0.5 done | Supabase creds (lyurcephjxokyhiclmgm) wired to .env.local. Vercel live at market-intel-n5xz797lw-kuriandonyku-2996s-projects.vercel.app. All 4 env vars set (Production+Preview; Development locked on Hobby plan). Redeploy successful, Supabase banner gone. |
| 2026-05-09 23:57 | Phase 0 complete | All done-criteria met. Repo ✅ shadcn ✅ Mapbox style ✅ Vercel live ✅ Supabase wired ✅ Boundary data deferred ✅ Lovable sandbox-only ✅ |
| 2026-05-10 00:00 | P1.1 done | /motion-prototype route live. New deps: mapbox-gl, framer-motion, @types/mapbox-gl. Scaffold: MapboxScene (map lifecycle + flyTo), chapters.ts (3 chapters + QLD stub), MotionPrototype (scroll driver, label overlay, progress dots, swipe stub, See data button). tsc clean. |
| 2026-05-10 02:20 | P1.x done | Drill-down interaction. Extracted 14 areas + 252 suburbs from boundaries_preview.html → public/data/areas.geojson + suburbs.geojson. MapboxScene rewritten: area+suburb sources, fill/outline/label layers, hover feature-state, click→fitBounds→filter suburbs, backToAreas(). MotionPrototype: mode state, onAreaClick/onSuburbClick callbacks, back button. tsc clean. |
| 2026-05-10 02:30 | P1.x v2 done | Dimmed basemap interaction. Sydney metro bbox [150.66,-34.08,151.34,-33.58] computed from 9 hand-drawn areas. Layer stack: basemap → dim-overlay (0.82 opacity) → areas-fill/outline → suburbs-fill/outline/label. On load: hide label layers (stored for restore), add dim, fit to Sydney metro. Click area: fitBounds→moveend→restore labels+remove dim+hide areas+show suburbs filtered. Back: reverse. Hover tooltip via Mapbox Popup. tsc clean. |
| 2026-05-10 02:40 | P1.x v3 done | Replaced scroll-driven chapter system with click-driven 3-level drill (state/area/suburb). Removed: chapters.ts, scroll handler, swipe stub, See data button. Added: free pan/zoom, 14-area side legend (collapsible), clickable breadcrumb (Sydney › Area › Suburb), Up button top-left, Focus badge top-centre at suburb level, dashed active-area outline, suburb selection via feature-state with paint-property mode swap. Brightened dim 0.82→0.55. State bbox widened to [150.50,-34.55,151.95,-32.80] so Newcastle+Wollongong show as edge context. Files moved to /public/areas_smooth.geojson + /public/suburbs.geojson. tsc clean. |
| 2026-05-10 02:50 | P1.x v4 done | Hidden basemap at state level. Removed dim-overlay; basemap layers fully hidden via setLayoutProperty visibility=none on every non-custom layer. Added /public/nsw_outline.geojson (62KB MultiPolygon from rowanhogan/australian-states MIT) — fill #1a1a1a@0.18, line #444@1px. State view = pure abstract dark canvas with NSW silhouette + 14 area shapes (fill 0.20, hover 0.42, line 3, hover 4). Drill into area: snap-toggle basemap visible, hide NSW + areas, show suburbs. Up to state: reverse. Up button label simplified to "← Up one level". Note: snap visibility toggle, no fade — fade was a stretch goal. tsc clean. |
| 2026-05-10 09:32 | P1.x v5 done | Click-driven drill + state toggle + cyberpunk aesthetic + suburb-level pitch. New cyberpunk palette (14 colours), NSW filter excludes Newcastle/Wollongong/North North/Queensland/Tasmania → 9 NSW areas. Suburb-level pitch 35° via flyTo({pitch}); state/area pitch 0. Hover lift via glow halo (line-blur 14, line-width 18, fill 0.55) instead of fill-extrusion since extrusion is invisible at pitch 0 — flagged trade-off. Tooltip shows area name in its colour + "{N} suburbs · click to drill in". State toggle (NSW/QLD/TAS) top-left: NSW = full drill model, QLD = Cranbrook single-suburb at pitch 35°, TAS = Launceston same. ← BACK button visible only at NSW area/suburb. Cyberpunk styling: bg #05050a, panels rgba(10,10,18,0.96)+#1a1a3a border+blur, neon cyan headings, gradient pills for active state + focus badge. Popup restyled #0a0a14 + #00e5ff border + cyan glow. NSW-only legend with neon dots. Breadcrumb shows state code. tsc clean, no lint errors. |
| 2026-05-10 09:42 | P1.x · auth gate adjusted, /motion-prototype now public | Inverted lib/supabase/proxy.ts from default-deny to default-allow. PROTECTED_PREFIXES allowlist = ["/protected", "/cda"]. Public portal pages (home, /motion-prototype, future /[state]/[area]/[suburb], /insights/*) render without a session. /protected (starter demo) and future /cda routes still redirect to /auth/login if unauthenticated. Note: starter uses proxy.ts (Next.js 16) not middleware.ts. tsc clean. |
| 2026-05-10 09:51 | P1.x v6 done | Multiple visual + functional fixes. (1) Replaced fragile basemap-hiding (which logged "0 layers" against the custom Dark 2D style) with a #05050a state-overlay world-polygon layer above the basemap; toggled visible at state, hidden at area/suburb. Bulletproof. (2) Solid tile stack — areas-base (#05050a 1.0 plate fully blocks basemap inside the tile boundary) + areas-glow (line-blur 6→16 hover, line-width 10→22 hover) + areas-tint (palette colour 0.50→0.70 hover) + areas-outline (4px→6px hover, 100% opacity). (3) Fixed pitch — switched from fitBounds (pitch ignored on this Mapbox version) to cameraForBounds → flyTo, which now reliably applies pitch:35 at suburb level, pitch:0 at area/state. (4) Fixed QLD/TAS — overlay hidden so basemap shows through, NSW chrome hidden, single suburb filtered + selected, basemap shows real-world context. (5) Suburb labels — pre-computed bbox-centroid Point source so MultiPolygon suburbs get exactly one label. NSW outline brightened (fill 0.5→0.9). tsc clean. |
| 2026-05-10 10:02 | P1.x v7 done | State-level visibility fix — v6's world-polygon overlay rendered fully opaque and (browser-reported) hid all area tiles. Switched to type:"background" layer at #05050a@0.88 opacity → basemap leaks through at ~12%, Australian coastline silhouette visible as spatial context. NSW outline tuned to spec (fill #0d0d18@0.4, line #2a2a3e 1.5px @0.9). Areas-glow refined: line-blur 8/16, line-width 14/24, opacity 0.6/0.95. Added areas-label-active layer — single uppercase area-name label at pre-computed centroid, colour matches palette, visible only when drilled into that area. Defensive map.setPitch(35) on suburb-drill moveend in case flyTo's pitch arg gets overridden. tsc clean. |
| 2026-05-10 10:09 | P1.x v8 done | NSW state-level dim halved per visual feedback (too dark). STATE_DIM_OPACITY 0.88 → 0.44; ~56% basemap shows through. Tiles + outlines untouched. |
| 2026-05-10 10:14 | P1.x v9 done | Tiles rendering as solid black — fix. (1) Hard-coded 14-name AREA_COLOR_BY_NAME mapping (replaces dynamically-generated palette index) so the colour match expression is bulletproof regardless of feature ordering. (2) Replaced flaky `["in", get(name), literal[]]` filter with explicit `all + !=` chain to exclude 5 algorithmic areas. (3) Bumped tint opacity 0.50→0.55 default, 0.70→0.75 hover (matches spec). (4) Explicit map.moveLayer() pass after add to enforce bottom→top z-order: base → glow → tint → outline. (5) Diagnostic console.log of all 4 area layer IDs + full style.layers id list on load for verification. tsc clean. |
| 2026-05-10 10:18 | P1.x v10 done | Tiles still solid black after v9 — root-cause refactor. The 4-layer base+tint stack was structurally fragile (any silent expression failure on the tint layer = the user only sees the black base). v10 replaces it: (1) Pre-bake `_color` and `_isNsw` into each feature's properties client-side BEFORE addSource — eliminates `match` expression at runtime entirely. (2) Collapse base+tint into a SINGLE `areas-fill` layer at fill-opacity 0.92→1.0 (hover) using palette colour directly via `["get", "_color"]`. No intermediate black plate; the colour itself blocks the basemap. (3) Filter is trivial `["==", ["get", "_isNsw"], true]`. (4) 3-layer area stack (glow → fill → outline) is simpler and harder to break. (5) Same enrichment applied to suburbs source + label centroid source. Diagnostic logs (layer existence, sample feature properties, full layer order) retained. tsc clean, no lint errors. |
| 2026-05-10 10:55 | P1.x v11 done | Two critical fixes. (1) Eastern Suburbs flying to London — root cause: drillToArea / focusSuburb / upToArea / showSingleStateSuburb were reading `(getSource("areas") as any)._data` to look up features. `_data` is undocumented Mapbox internal, unreliable across versions. Replaced with `areasFCRef` / `suburbsFCRef` populated at load time. Added bbox sanity check in flyToBbox (rejects invalid lng/lat ranges, logs loudly) + bbox console.log per drill so the value is visible. Switched flyToBbox to pass bounds in `[[sw], [ne]]` form (unambiguous across versions). (2) Tiles still rendering black — hardened paint expression. Renamed enriched properties from `_color`/`_isNsw` → `tileColor`/`tileNsw` (some Mapbox versions handle underscore-prefixed properties oddly). Wrapped `["get","tileColor"]` in `coalesce(`with `match` on `name` as fallback) so colour resolves even if the enriched property isn't accessible. Filter likewise gets a name-based fallback via `any/all`. Added `queryRenderedFeatures(areas-fill)` diagnostic 1.5s post-load that prints the rendered feature count + sample properties. tsc clean. |
| 2026-05-10 11:25 | Director instrumentation | Switched out of fix-mode per Director: stopped applying patches, added 3 targeted diagnostic checks (ref freshness on click, load timing, style center/zoom/projection) so root cause could be identified from runtime evidence instead of guesses. Console output confirmed: data path 100% clean (refs populated before clicks, lookup matches, bbox correct Sydney coords, flyTo lands correctly). Issue isolated to RENDERING only. |
| 2026-05-10 11:30 | P1.x v12 done | Multi-fix per Director's rendering analysis. (1) Layer z-order — explicit `moveLayer` reduce-pass walks custom stack bottom→top and forces order: state-dim-overlay → nsw-fill → nsw-line → areas-glow → areas-fill → areas-outline → areas-outline-active → areas-label-active. Eliminates any fragility from addLayer order. (2) STATE_DIM_OPACITY 0.44→0.94 — basemap silhouette readable, road/place labels not. (3) Paint expressions stripped to bare minimum — replaced `coalesce(get(tileColor), match(get(name)))` with plain `match(get(name))`. `name` is the source-of-truth property used by `promoteId` and click handlers, so it's always accessible; no enrichment-property dependency. Filter also flattened to plain `all + !=` chain (no enrichment dependency). (4) Suburb fill defaults bumped — area-mode 0.15→0.25 (0.55 hover), suburb-mode 0.06→0.08, line-width 1.6→1.8 default — polygons now read as solid swatches. (5) Pitch enforcement hardened — new `enforcePitch()` helper applies setPitch on `moveend` AND `idle` AND a 1.4s setTimeout fallback (was: single `moveend`). Also passed `pitch` + `bearing` to fitBounds fallback in case cameraForBounds returns null. (6) `goToNswState` now clears feature-state.hover on every NSW area on entry — prevents the "1 cyan tile, 13 black" symptom Director observed (stale feature-state from previously-clicked tile). tsc clean. |
| 2026-05-10 11:39 | P1.x v13 — STATE LEVEL FULL RESET (steps 1+2) | Patch-and-iterate exhausted; tiles still rendered black. Director's strip-and-rebuild plan. STRIPPED from state level: state-dim-overlay (background), nsw source + nsw-fill + nsw-line, areas source + areas-glow + areas-fill + areas-outline + areas-outline-active + areas-label-active, the layer-order moveLayer reduce-pass, hover feature-state plumbing on areas, all custom match/coalesce paint expressions, hover popup tooltip. ADDED: ONE source `areas-simple` (URL `/areas_smooth.geojson`) and ONE layer `areas-simple-fill` (fill, hardcoded `#00e5ff`, opacity 0.5, fill-outline-color `#00ffff`). Click + cursor-only hover wired on areas-simple-fill so drill-into still works. Drill-level untouched: suburbs source + 3 suburb layers, drill camera handlers (drillToArea/focusSuburb/upToArea/showSingleStateSuburb) intact. Toggle helpers (setOverlayVisible/setNswVisible/setAreasVisible/setActiveAreaDashed/setActiveAreaLabel) made safe — guarded by `getLayer` check, no-op when stripped layer absent. setAreasVisible now toggles `areas-simple-fill`. Diagnostic logs: layer-existence check + `queryRenderedFeatures` count 1.5s post-load. PAUSED before steps 4a-4e — awaiting user visual verification that the simplest-case render (14 cyan blobs) is on screen. |
| 2026-05-10 11:50 | SMOKING GUN found | User instrumented `map.getProjection()` and `map.getStyle().center` in the runtime — saved Mapbox Studio style ships with `projection: "globe"` and `center: [-0.09, 51.50]` (London-Greenwich). Globe projection treats fill polygon paint differently → dark blobs instead of cyan tints regardless of paint config; saved London center hijacks any failed camera fallback path. Single root cause for: London flyTo, black tiles, missing labels, inconsistent fix iterations. |
| 2026-05-10 12:05 | P1.x v14.1 — visual polish per Director spec | Two visual fixes only, no architecture changes. (1) Tile paint properties locked to spec — areas-fill `fill-opacity`: 0.30 default / 0.50 hover (was 0.35/0.55). areas-outline `line-width`: 4 default / 6 hover (was 4/5). areas-glow tuned to "optional" spec — line-blur 6/12, line-width 14/22, line-opacity 0.5/0.9. (2) AREA_COLOR_BY_NAME palette shifted for North/South/West to higher-pop colours (#7e57c2 / #d500f9 / #ff4081) per Director's exact match expression — was indigo cluster, now spreads to purple/magenta/pink for visual distinction. Non-NSW areas (Newcastle, Wollongong, North North) re-shuffled to fill the freed indigo slots. (3) Hover banner — replaced `mapboxgl.Popup` with vanilla DOM cursor-following div appended to map container. Banner has per-tile coloured border (1px solid {areaColor}) + per-tile coloured glow shadow (0 0 20px {areaColor}@0.4), positioned at `e.point.x + 14, e.point.y - 12`. Content: area name in tile colour (12px, weight 700, letter-spacing 0.5px) + "{N} suburbs · click to drill in" subtitle (10px, weight 500, #7a8aae). Hidden on mouseleave + on level transitions (replaces the popupRef remove pattern). (4) MotionPrototype `colorFor` switched from `CYBERPUNK_PALETTE[idx]` to `AREA_COLOR_BY_NAME[name]` so legend dots match map paint exactly after the palette shift. AREA_COLOR_BY_NAME exported. tsc clean, no lint errors. |
| 2026-05-10 11:53 | P1.x v14 — projection fix + cyberpunk stack restored | (1) `map.setProjection("mercator")` called unconditionally as the first thing inside `map.on("load")` — overrides the saved globe default before any rendering. (2) Stripped `areas-simple` source + `areas-simple-fill` layer (the v13 strip-down). (3) Re-added cyberpunk stack with LIGHT TINT (per Director — not solid plate): state-dim-overlay (bg, #05050a @0.94) → nsw-fill/nsw-line (faint silhouette) → areas-glow (line, blur 8/16, width 14/24, opacity 0.6/0.95) → areas-fill (fill 0.35/0.55 hover) → areas-outline (line, width 4/5 hover, opacity 1.0) → areas-outline-active (dashed, hidden) → areas-label-active (symbol, hidden). All colours via `match(get(name)) → palette`. (4) Hover banner restored: cursor-following popup with area name + suburb count, cyan border via existing `.cyber-tooltip` styles. (5) Click handler retargeted areas-simple-fill → areas-fill. (6) Hover feature-state plumbing restored on areas-fill (mousemove sets hover:true, mouseleave clears). (7) `goToNswState` clears stale hover feature-state on entry. (8) Filter is plain `all + !=` chain excluding the 5 algorithmic boxes. tsc clean. |
| 2026-05-10 12:48 | Motion prototype | SuburbSearch wiring + `navigateSuburbFromSearch` in MotionPrototype: search picks run state → NSW `goToNswState` when needed → `drillToArea` → `focusSuburb` (timeouts ~1480ms to follow 1400ms fly); QLD/TAS picks call `showSingleStateSuburb` + UI state. SuburbSearch prop `onNavigateToSuburb`. `npm run build` clean. |
| 2026-05-10 13:15 | Motion prototype | Aceternity `EncryptedText` at `components/aceternity/encrypted-text.tsx`; top-centre brand reveal "Market Meerkat" on motion prototype; `/motion-prototype` wrapped in `<Suspense>` for Next prerender + `Math.random` in client. Focus pill `z-[1050]` above brand `z-[1000]`. `npm run build` clean. |
| 2026-05-10 14:00 | Motion prototype | Full-page `IntroSplash` on `/motion-prototype` (#000 fullscreen, centred 48 px cyan glow); decrypt → 1 s dwell → 600 ms opacity fade → unmount + optional `onComplete`. `EncryptedText` defaults slowed 25 % (~62 ms), `onRevealComplete` hook. Removed top-centre header; Suspense wraps IntroSplash + MotionPrototype. |
| 2026-05-16 | P1.x v2 scaffold | `/v2` duplicates motion prototype via `motion-v2/*V2.tsx` + `lib/palette/v2.ts` (passthrough to `components/motion/areaPalette.ts`). Cyberpunk originals untouched. `npm run build` clean — route `/v2` static prerendered. |
| 2026-05-29 18:26 | T1 done | Weekly supply percentile chart: p10–p70 filled band + p50 median line (SupplyPercentileChart). |
| 2026-05-29 18:27 | T2 done | Listing type mix: compact card height + smaller donut/stacked bar footprint. |
| 2026-05-29 18:28 | T3 done | Area suburb leaderboard: client-side sort toggles (rank default, high→low metrics). |
| 2026-05-29 18:28 | T4 done | Suburb dashboard: removed Supply vs Demand panel (chart deleted; fetch unchanged). |
| 2026-05-29 18:35 | T5 done | Fast-forward merged g3-data-wireup → main; production deploy triggered on Vercel. |
| 2026-05-29 18:46 | C1 done | Deleted remote `motion-prototype-stable-baseline` — 0 commits outside g3-data-wireup (bcf7423…b494e10 confirmed). |
| 2026-05-29 18:47 | C2 done | Added `npm run verify` (`next build`) + README pre-push gate for main / g3-data-wireup. |
| 2026-05-29 19:10 | fix | Recharts marks: explicit hex series colours + style backup + CSS fallback (undefined INK_* / currentColor). |
| 2026-05-29 19:29 | fix | ChartViewport: mount gate + numeric ResponsiveContainer height — charts paint on first load without resize. |
| 2026-05-29 20:05 | fix | ChartViewport: drop ResponsiveContainer; ref + ResizeObserver → explicit chart width/height (first-paint fix). |
| 2026-05-29 21:15 | fix | M1-M8 UI audit pass: NSW map init+drill, leaderboard numbering, top-10 suburb mix, naming/range/empty-state copy, longevity column hide, supply band opacity. |
| 2026-05-29 21:30 | fix | ClassificationPill: display NO_DATA as LOW VOL (demand ungauged when supply < 2). |
| 2026-05-29 22:10 | fix | useMeasuredWidth + ChartViewport: ResizeObserver-driven width, placeholder until width>0, explicit Recharts dimensions (first-paint reliability). |
| 2026-05-29 22:25 | feat | Redirect `/` → `/v2`; root metadata title "Market Meerkat". |
| 2026-05-29 23:00 | refactor | Drop `/v2` URL prefix — `app/(app)` route group at `/`, `[state]/[area]`, `[suburb]`; permanent `/v2` redirects. |
| 2026-05-29 23:20 | fix | MapboxSceneV2: restore state-dim-overlay + layer z-order; toggle dim at state/area drill (white glow on dark canvas). |
| 2026-05-29 23:45 | fix | MapboxSceneV2: remove global dim overlay; white-glow area+suburb outlines on normal basemap (layer order + fill-outline fix). |
| 2026-05-30 00:10 | fix | MapboxSceneV2: ensureBasemapVisible + custom-layer-only toggles; slot:top layers (tiles load, basemap stays visible). |
| 2026-05-30 00:35 | refactor | Switch to mapbox dark-v11; drop Standard-v3 slots/setProjection/ensureBasemapVisible workarounds. |
| 2026-05-30 01:15 | fix(map) | Revert MapboxSceneV2 init to def54ba (setProjection mercator, env style required); white-glow outlines via INK_100 + line-blur. |
| 2026-05-30 12:00 | feat(map) | Port / map to Leaflet 1.9.4 + CartoDB dark basemap + SVG drop-shadow glow; drop MapboxSceneV2 from v2 route. |
| 2026-05-30 13:00 | fix(map) | Hide non-active areas on drill; suburb divIcon labels; suburb-click zooms, See-data navigates. |
| 2026-05-30 14:30 | fix(map) | Hover-only suburb labels; tight suburb-focus zoom + dim siblings; areas visibility:hidden when drilled. |
| 2026-05-30 15:45 | fix(map) | Suburb re-focus (dim siblings stay clickable); suppress SVG glow during zoom; zoom bottom-right; NSW label. |
| 2026-05-30 16:30 | fix(map) | Dimmed-but-visible neighbours at suburb focus (stroke 0.3); ease focus zoom padding 120 + maxZoom 14. |
| 2026-05-29 18:00 | fix(map) | Leaflet perf: defer suburbs until fly end, smoothFactor 2.5, SVG padding 4, persistent decluttered labels, cursor z-index 99999. |
| 2026-05-29 19:30 | chore(release) | Tagged `v0.1-map-working` on main; created `dev` branch. Workflow: feature/fix on `dev` (Vercel Preview) → merge to `main` when verified (production) → annotated tag per release (`v0.2`, …). |
| 2026-05-29 21:00 | feat(map) | WarpBackground transition mask on drill/back; `@turf/point-on-feature` visual-centroid suburb labels. |
| 2026-05-29 22:30 | feat(map) | Uniform CSS blur on `.leaflet-container` during zoom/fly; dropped WarpBackground map overlay (kept for future See-data hop). |
| 2026-05-29 23:15 | fix(map) | Clear `dashArray` on all area paths when returning to state level (solid outlines). |
| 2026-05-30 00:00 | fix(map) | Reliable state-level solid areas: `dashArray: null`, post-zoom refresh, recreate area GeoJSON on Back. |
| 2026-05-30 01:30 | feat(ui) | `GatheringDataOverlay` — grayscale WarpBackground + eye-see-you Lottie on See data → dashboard (`dev` only). |
| 2026-05-30 02:15 | feat(ui) | Loader polish: ~2.5× center card, text inside box, thinner beams (`beamSize` 2), radial edge mask + solid card over warp center. |
| 2026-05-30 03:00 | feat(ui) | Loader ~3× card, 3.5s minimum duration, slower warp beams (`beamDuration` 4.5). |
| 2026-05-30 03:30 | feat(ui) | Loader 1500px centered card; flex-centered overlay + in-box eye/label. |
| 2026-05-30 04:00 | feat(ui) | Loader card scaled to 3/8 (~563px), padding/Lottie/label scaled; still flex-centered. |
| 2026-05-30 04:15 | feat(ui) | Loader card scaled to 1/10 of 1500px design (~150px). |
| 2026-05-30 04:30 | feat(ui) | Loader card wide rectangle: 150px length, halved Lottie height (~69px). |
| 2026-05-30 05:00 | feat(ui) | Loader: remove box; restore 1500px eye viewport; single-line label. |
| 2026-05-30 05:15 | feat(ui) | Loader: 3× eye viewport, 2× label size, no box. |
| 2026-05-30 06:00 | fix(ui) | Restore visible eye (`min(92vw,72dvh)`); remove broken scale(4); warp 1.3× speed. |
| 2026-05-30 06:30 | release | Merged `dev` → `main` — GatheringData loader + map fixes live on marketmeerkat.guru. |
| 2026-05-31 02:22 | auth gate | Flipped lib/supabase/proxy.ts to login-required (public allowlist /auth only; /auth/sign-up → login). Disabled sign-up links; login redirects to /. AuthButton + logout in app layout corner. npm run verify clean; dev preview for login-flow QA. |
| 2026-05-31 02:45 | auth UI | Brutalist B&W /auth pages (black bg, white card/inputs/button). Meerkat peek Lottie on login — greyscale, random edge every 7.5s, behind form. Bundled public/lottie/meerkat-looking-around.json. npm run verify clean; pushed dev. |
| 2026-05-31 03:10 | auth UI | Meerkat peek refined: edge-matched slide axis + rotation (left/right horizontal, bottom vertical); 3-cycle emerge pattern full(+20%) → half(+50%) → quarter(+100%). npm run verify clean; pushed dev. |
| 2026-05-31 03:25 | auth UI | Meerkat peek scales ×1.5 on all three emerge cycles (1.8 / 2.25 / 3.0). npm run verify clean; pushed dev. |
| 2026-05-31 03:40 | auth UI | Session auth chrome auto-dismisses after 10s (sessionStorage, no reappear). Meerkat cycle scales: base 1.8 → +20% (2.16) → +50% (2.7). npm run verify clean; pushed dev. |
| 2026-05-31 03:50 | auth UI | Meerkat cycle 2/3 scales locked to +20% / +50% vs cycle 1 (2.16 / 2.7). npm run verify clean; pushed dev. |
| 2026-05-31 04:00 | auth UI | Meerkat cycle 2/3 scales → +30% / +70% vs cycle 1 (2.34 / 3.06). npm run verify clean; pushed dev. |
| 2026-05-31 04:15 | auth | Tab-scoped sessions via sessionStorage `mm_session_alive` — TabSessionGate signs out on fresh tab; reload keeps session. Login/confirm/password flows set marker. npm run verify clean; pushed dev. |
| 2026-05-31 04:30 | release | Merged `dev` → `main` (`d5ab334`) — site-wide auth gate + inverted login UI, meerkat peek, 10s auth chrome, tab-close logout live on marketmeerkat.guru. Tagged `v0.2-auth-live`. npm run build + verify clean. |
| 2026-05-31 06:10 | chore | Full check pass: eslint ignore `.next`, lint nits fixed, local QA screenshots gitignored; `npm run build` + verify + tsc clean; pushed all branches/tags to origin. |
| 2026-08-01 19:40 | P4 | Suburb Explorer + Area Analytics rebuilt on the Phase-3 ISO-week tables (32/23 metric cards, week nav, gap-aware trends, confidence + coverage badges, vs-Sydney strip, leaderboard WoW). QA gate green: data harness ALL CHECKS PASSED, Playwright pass clean at 1440/390, verify + tsc + lint clean. |
| 2026-08-01 19:55 | release | Merged `dev` → `main` (`80f8ff0`) — Phase-4 analytics UI live on marketmeerkat.guru (`dpl_8guBBaze…`, READY). Prod re-verified with the screens harness: 8/8 URLs clean at 1440/390. PHASE4_REPORT.md written. |
| 2026-08-01 20:50 | P4 followup | Scoped shell exception: DashboardShell header stacks on phone (brand / Dashboard · G3 / Snapshot) with each label kept whole — fixes the `DASHBOARDSNAPSHOT` collision at 390px. Desktop row layout unchanged from `sm` up. verify + tsc + lint clean; screens harness clean. |

| 2026-08-02 13:02 | R2 | Suburb Explorer Round 2 rework (bedroom+tier filter, expanders, composition/cohorts, C demand, F removed). QA green at 1440/390; verify clean. |
| 2026-08-02 14:02 | R3B | Suburb Explorer Round 3B — bed×tier `_x` filter page-wide, Movement first, banner composition strip + filter tags, A3/C14/D23/ladder removed, D24↑, white separators. QA green 1440/390; verify clean. |
